# 🚀 БЫСТРЫЙ ЗАПУСК SFA Secure File Program

## ⚡ Запуск одной кнопкой

### Windows
```bash
# Дважды кликните на файл:
start.bat
```

### Linux/macOS
```bash
# В терминале выполните:
./start.sh
```

## 📋 Что нужно установить

### 1. Node.js 18+
- Скачайте с: https://nodejs.org/
- Установите и перезагрузите компьютер

### 2. Python 3.8+
- Скачайте с: https://python.org/
- При установке отметьте "Add Python to PATH"

### 3. Python библиотека
```bash
pip install cryptography
```

### 4. Rust (только для Tauri)
```bash
# Windows
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Установите Tauri CLI
cargo install tauri-cli
```

## 🎯 Рекомендации

### Для начинающих (Electron):
```bash
cd electron-react
npm install
npm run electron-dev
```

### Для продвинутых (Tauri):
```bash
cd tauri-react
npm install
npm run tauri dev
```

## 🔧 Ручной запуск

### Tauri версия (Рекомендуется)
```bash
cd tauri-react
npm install
npm run tauri dev
```

### Electron версия
```bash
cd electron-react
npm install
npm run electron-dev
```

## ❓ Частые проблемы

### "Node.js не найден"
- Установите Node.js 18+ с https://nodejs.org/
- Перезагрузите компьютер

### "Python не найден"
- Установите Python 3.8+ с https://python.org/
- Отметьте "Add Python to PATH" при установке

### "cryptography не установлена"
```bash
pip install cryptography
```

### "Rust не найден" (для Tauri)
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
cargo install tauri-cli
```

## 📞 Поддержка

Если ничего не работает:
1. Запустите `start.bat` (Windows) или `./start.sh` (Linux/macOS)
2. Выберите "3. Проверить зависимости"
3. Установите недостающие компоненты
4. Попробуйте снова

---

**🎉 Готово! Теперь у вас есть современное приложение для шифрования файлов!** 